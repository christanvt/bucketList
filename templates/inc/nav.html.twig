<nav>
  <ul>
    <li><a href="{{ path('main_home') }}" title="Home">Home</a></li>
    <li>
      <a href="{{ path('wish_list') }}" title="Test"
        >Liste des choses à faire</a
      >
    </li>
    {% if app.user %}
    <li>
      <a href="{{ path('wish_create') }}" title="Test"
        >Créer une nouvelle choses à faire</a
      >
    </li>
    <li>
      <a href="{{ path('app_logout') }}"
        >Se déconnecter ({{ app.user.firstname }})</a
      >
    </li>

    {% else %}
    <li><a href="{{ path('app_login') }}">Login</a></li>
    <li><a href="{{ path('app_register') }}">Créer un compte</a></li>
    {% endif %}
    <li><a href="{{ path('main_aboutus') }}" title="Test">About us</a></li>
  </ul>
</nav>
